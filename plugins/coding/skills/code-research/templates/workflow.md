# 核心工作流

## 端到端业务流程

识别项目中 3-5 个最核心的业务流程，每个流程按以下结构说明：

### [流程名称，如 用户注册 / 订单处理 / 数据同步]

**它是什么**：[一句话说明这个流程完成什么业务目标]
**触发方式**：[用户操作 / 定时任务 / 外部事件 / API 调用]

**流程全景**（标注涉及的模块和关键文件）：

```mermaid
sequenceDiagram
    participant 用户/触发源
    participant 模块A
    participant 模块B
    participant 模块C
    participant 外部系统/存储

    用户/触发源->>模块A: [操作描述]（入口: 文件:行号）
    模块A->>模块A: [内部处理]（文件:行号）
    模块A->>模块B: [调用/传递]（文件:行号）
    模块B->>模块C: [调用/传递]（文件:行号）
    模块C->>外部系统/存储: [读写操作]（文件:行号）
    外部系统/存储-->>模块C: [返回结果]
    模块C-->>模块A: [返回结果]
    模块A-->>用户/触发源: [最终响应]
```

**关键步骤说明**：

| 步骤 | 负责模块 | 做什么 | 为什么在这一步 | 代码位置 |
|------|---------|--------|--------------|----------|
| 1 | [模块名] | [操作描述] | [为什么需要这一步] | `文件:行号` |
| 2 | ... | ... | ... | ... |

**异常分支**：

```mermaid
flowchart TD
    A[正常流程步骤N] --> B{异常判断条件}
    B -->|条件1| C[异常处理A]
    B -->|条件2| D[异常处理B]
    C --> E[恢复/终止]
    D --> E
```

| 异常场景 | 触发条件 | 处理方式 | 对用户的影响 |
|---------|---------|---------|-------------|
| [场景描述] | [什么条件下触发] | [重试/降级/报错/忽略] | [用户看到什么] |

---

## 模块内部执行流程

对系统中最核心的 2-3 个模块，深入展示其内部执行逻辑：

### [模块名称] 内部流程

**它是什么**：[这个模块的核心职责]
**为什么需要详细了解**：[这个模块的复杂度在哪里，为什么不能当黑盒看待]

**执行流程**：

```mermaid
flowchart TD
    A[入口: 函数名] --> B[步骤1: 描述]
    B --> C{判断条件}
    C -->|条件A| D[分支A处理]
    C -->|条件B| E[分支B处理]
    D --> F[步骤3: 描述]
    E --> F
    F --> G{是否需要循环}
    G -->|是| B
    G -->|否| H[返回结果]
```

**关键决策点**：

| 决策点 | 判断条件 | 影响 | 代码位置 |
|--------|---------|------|----------|
| [决策描述] | [什么条件决定走哪条路] | [不同分支导致什么不同结果] | `文件:行号` |

**状态转换**（如果模块涉及状态管理）：

```mermaid
stateDiagram-v2
    [*] --> 初始状态
    初始状态 --> 状态A: 触发事件1
    状态A --> 状态B: 触发事件2
    状态B --> 状态A: 回退条件
    状态B --> [*]: 完成条件
```

| 状态 | 含义 | 进入条件 | 退出条件 |
|------|------|---------|---------|
| [状态名] | [代表什么] | [什么条件进入] | [什么条件离开] |

---

## 流程间的关联

[说明上述流程之间是否有依赖、共享状态、或触发关系]

```mermaid
graph LR
    流程A -->|触发| 流程B
    流程A -->|共享数据| 流程C
    流程B -->|回调通知| 流程A
```
